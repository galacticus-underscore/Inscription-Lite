/**
* Locations.java
* 
* declares the different cells game screen where images are to be incorporated
* such as the grid where cards are placed, the deck and the graveyard.
**/

package models.enums;

public enum Locations {
    HA HD, HH, H1, H2, H3, H4, HG,
    AA, AD, AH, A1, A2, A3, A4, AG;
}
